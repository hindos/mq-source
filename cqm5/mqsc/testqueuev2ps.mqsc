* Define test queue used to validate queue manager by tekton pipeline
DEFINE QLOCAL(TEST.QUEUE.PS.V2) REPLACE
SET AUTHREC PROFILE(TEST.QUEUE.PS.V2) GROUP('apps') OBJTYPE(QUEUE)  AUTHADD(PUT, GET, PASSALL, BROWSE, INQ)